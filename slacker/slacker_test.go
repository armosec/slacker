package slacker

import "testing"

func Test(t *testing.T) {
	// bot, err := SlackBotInit("", "xox", true)
	// if err != nil {
	// 	t.Errorf("failed to initialize bot due to: %v", err.Error())
	// }
	// chanid, err := bot.FindChannel("#general")
	// if err := bot.SendCriticalMessage(chanid, "scan failed"); err != nil {
	// 	t.Errorf("failed to send message due to: %v", err.Error())
	// }
}
